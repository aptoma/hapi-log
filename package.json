{
  "name": "@aptoma/hapi-log",
  "author": "Martin Jonsson <martin@aptoma.com>",
  "version": "3.0.1",
  "description": "Hapi Log Plugin",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git@github.com:aptoma/hapi-log.git"
  },
  "scripts": {
    "lint": "eslint --ext '.js' lib test '*.js'",
    "test": "npm run lint && NODE_ENV=test istanbul cover -i 'lib/**/*.js *.js' _mocha -- -u exports -R spec --timeout 3000 'test/**/*.test.js'",
    "watch": "mocha --watch 'test/**/*.js' --timeout 500",
    "ci": "npm test --coverage && istanbul report cobertura",
    "release": "npm test && release-it -n -i patch",
    "release:minor": "npm test && release-it -n -i minor",
    "release:major": "npm test && release-it -n -i major"
  },
  "engines": {
    "node": ">=4.0.0"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "hoek": "^2.14.0",
    "json-stringify-safe": "^5.0.1",
    "moment": "^2.10.6"
  },
  "devDependencies": {
    "@aptoma/eslint-config": "^5.0.0",
    "eslint": "^3.8.1",
    "hapi": "^10.1.0",
    "istanbul": "^0.3.8",
    "mocha": "^2.2.1",
    "nsp": "^2.0.2",
    "release-it": "^2.0.3",
    "should": "^7.0.1",
    "sinon": "^1.16.1"
  }
}
